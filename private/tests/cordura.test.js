describe('Prueba de Cordura', () => {
  test('debería pasar una prueba de veracidad básica', () => {
    expect(true).toBe(true);
  });

  test('debería poder hacer matemáticas básicas', () => {
    expect(1 + 1).toBe(2);
  });
});
